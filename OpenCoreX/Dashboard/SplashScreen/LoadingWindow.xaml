<Window x:Class="OpenCoreX.Dashboard.SplashScreen.LoadingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Loading" Height="300" Width="500"
        WindowStyle="None" ResizeMode="NoResize"
        AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Dashboard/Styles/DashboardStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
            
            <Storyboard x:Key="FadeOutAnimation">
                <DoubleAnimation Storyboard.TargetProperty="Opacity" From="1.0" To="0.0" Duration="0:0:0.5" />
            </Storyboard>
        </ResourceDictionary>
    </Window.Resources>

    <Border Background="{StaticResource WindowBackgroundBrush}" CornerRadius="16" BorderBrush="#1C2232" BorderThickness="1">
        <Grid>
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="OpenCoreX" FontSize="36" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center">
                    <TextBlock.Effect>
                        <DropShadowEffect Color="#5B9BFC" BlurRadius="20" ShadowDepth="0" Opacity="0.4"/>
                    </TextBlock.Effect>
                </TextBlock>
                <TextBlock Text="System Diagnostic Tool" FontSize="14" Foreground="{StaticResource SubtleTextBrush}" HorizontalAlignment="Center" Margin="0,5,0,30" />
                
                <Grid Width="350">
                    <!-- Custom Rounded Progress Bar Track -->
                    <Border Height="6" Background="#1C2232" CornerRadius="3" />
                    <!-- Progress Bar Indicator -->
                    <Border x:Name="ProgressIndicator" HorizontalAlignment="Left" Width="0" Height="6" Background="{StaticResource AccentBrush}" CornerRadius="3">
                        <Border.Effect>
                            <DropShadowEffect Color="#8F6BFF" BlurRadius="10" ShadowDepth="0" Opacity="0.5"/>
                        </Border.Effect>
                    </Border>
                </Grid>
                
                <TextBlock x:Name="StatusText" Text="Initializing..." Foreground="{StaticResource SubtleTextBrush}" FontSize="12" HorizontalAlignment="Center" Margin="0,15,0,0" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
